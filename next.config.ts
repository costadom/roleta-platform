import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Caso tenha imagens configuradas ou outras coisas, mantenha. 
     Apenas APAGUE o bloco que diz "eslint: { ... }" */
};

export default nextConfig;
